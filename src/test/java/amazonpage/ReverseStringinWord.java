package amazonpage;

public class ReverseStringinWord {
	static final int  Maxchsr=256;
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*
		 * String str = "Sandeep"; int[] count = new int[Maxchsr]; int len =
		 * str.length(); for(int i=0;i<len;i++) count[str.charAt(i)]++; char[] ch = new
		 * char[str.length()]; for(int i=0;i<len;i++) { ch[i] = str.charAt(i); int
		 * find=0; for(int j=0;j<i;j++) { if(str.charAt(i)==ch[j]) { find++; } }
		 * 
		 * if(find==1) { System.out.println(str.charAt(i)+": "+count[str.charAt(i)] ); }
		 * 
		 * }
		 */
		
		

		
		
		
			
		

	
    }
}

